import * as React from "react";
import { Layout } from "./views";

export const App = () => {
    return (
        <Layout>
            <header>Helpful Human: Colors</header>
        </Layout>
    );
}